name: "Workflow example"
on:
  schedule:
    - cron: '* * * * *'

jobs:
  hello:
    name: "Say hello"
    runs-on: ubuntu-latest
    steps:
      - name: Display message
        run: echo "Hello from GitHub Actions!"